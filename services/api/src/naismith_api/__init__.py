"""Naismith API + session gateway.

This is the Phase 0/1 vertical slice described in the governing handoff
(`docs/NAISMITH_CLAUDE_CODE_HANDOFF.md`, §14): a text-first conversation core
that proves the governance skeleton — constitution surfacing, a deterministic
policy engine outside the model, a mock model adapter, and an append-only audit
trail — before any autonomy, memory, voice, or external tools are introduced.
"""

__version__ = "0.1.0"
